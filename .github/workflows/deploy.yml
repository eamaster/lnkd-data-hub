name: Deploy to Production

on:
  workflow_dispatch:  # Only run manually, not on every push

jobs:
  test-deployment:
    name: Test Production Deployment
    runs-on: ubuntu-latest
    steps:
      - name: Test Worker Health
        run: |
          curl -f https://lnkd-data-hub-proxy.smah0085.workers.dev/api/search/jobs?q=test&limit=1 || exit 1
      
      - name: Notify Success
        run: echo "✅ Worker is healthy and running!"

